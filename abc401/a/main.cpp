#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (int)(n); ++i)
using namespace std;
using ll = long long;

int main() {
    int x;
    cin >> x;

    if (200 <= x && x <= 299) {
        cout << "Success" << endl;
    } else {
        cout << "Failure" << endl;
    }
}
